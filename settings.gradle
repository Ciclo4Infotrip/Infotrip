dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    def var = repositories {
        google()
        mavenCentral()

    }
    var
}
rootProject.name = "Infotrip"
include ':app'
include ':jardin'
